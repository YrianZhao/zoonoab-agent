# 🚀 一键部署 - 超简单教程

## 📦 第一步：打包文件

1. 打开文件夹：`C:\Users\xlion\Desktop\ZoonoAb\ZoonoAb\zoonoab-agent`
2. 按 **Ctrl+A** 全选所有文件
3. 右键 → **发送到** → **压缩 (zipped) 文件夹**
4. 得到 `zoonoab-agent.zip`

---

## 🌐 第二步：创建 GitHub 仓库

1. 访问 https://github.com
2. 登录你的 GitHub 账号（如果没有，点击右上角 "Sign up" 注册）
3. 点击右上角 **+** → **New repository**
4. 填写：
   - **Repository name**: `zoonoab-agent`
   - 选择 **Public** ✅
   - **不要** 勾选 "Add a README file"
5. 点击 **Create repository**

---

## 📤 第三步：上传文件到 GitHub

1. 在刚创建的仓库页面，点击 **uploading an existing file**
2. 打开你刚才打包的 `zoonoab-agent.zip`
3. **解压**后，**全选所有文件**
4. **拖到** GitHub 上传页面
5. 等待上传完成
6. 在 "Commit changes" 框中输入：`Initial commit`
7. 点击 **Commit changes**

---

## 🚀 第四步：部署到 Render

项目里已经准备好了 `render.yaml`，最推荐用 Render Blueprint 部署。

1. 访问 https://dashboard.render.com
2. 点击 **New +** → **Blueprint**
3. 连接 GitHub，并选择你的 `zoonoab-agent` 仓库
4. Render 会自动读取根目录的 `render.yaml`
5. 在创建页面填写两个密钥：
   ```text
   VOICE_ASR_API_KEY=你的 SiliconFlow API Key
   ASSISTANT_CHAT_API_KEY=你的 SiliconFlow API Key
   ```
6. 点击 **Apply** 或 **Create Resources**
7. 等待部署完成

`render.yaml` 会自动配置：

```text
Runtime: Node
Build Command: npm ci
Start Command: npm start
Health Check Path: /api/health
Node Version: 20.19.2
语音 ASR: SiliconFlow SenseVoiceSmall
兜底聊天: SiliconFlow Qwen/Qwen3-32B
持久磁盘: /var/data
```

持久磁盘很重要：页面里保存的 API 配置会写到后端 `/var/data/voice-api-config.json`，不会明文保存在前端，也不会因为普通重启丢失。

---

## 🌍 第五步：获取公开访问链接

1. 部署完成后，进入 Render 的 `zoonoab-agent` 服务页面
2. 页面顶部会显示公开访问链接
4. 复制你的公开访问链接，例如：
   ```
   https://zoonoab-agent.onrender.com
   ```

---

## ✅ 第六步：测试访问

1. 打开浏览器
2. 粘贴刚才复制的链接
3. 打开健康检查地址：`https://你的-render域名/api/health`
4. 看到下面返回 = **后端成功**
   ```json
   {"ok":true,"platform":"ZoonoAb","sessions":0}
   ```
5. 再打开主页，看到页面正常加载 = **成功！** 🎉
4. 把链接分享给别人，他们也能访问了！

---

## 🎙️ 第七步：填写语音和聊天 API

页面输入框右侧有一个很小的 **API** 按钮，点击后填写两套配置：

```text
语音识别 ASR
Base URL: https://api.siliconflow.cn/v1/audio/transcriptions
API Key: 你的 SiliconFlow API Key
Model: FunAudioLLM/SenseVoiceSmall

兜底聊天 Chat
Base URL: https://api.siliconflow.cn/v1/chat/completions
API Key: 你的 SiliconFlow API Key
Model: Qwen/Qwen3-32B
```

注意：`FunAudioLLM/SenseVoiceSmall` 只做语音转文字，不能作为聊天兜底模型。兜底聊天要填写 `/v1/chat/completions` 接口和聊天模型。两套 Key 不会写入浏览器本地存储，保存后会写入后端私有配置文件 `.runtime/voice-api-config.json`，该目录已被 Git 忽略；刷新页面或短期 session 过期后，后端仍会继续使用这份配置。

填写后可以先点 **测试语音** 和 **测试聊天**。测试语音会临时录制约 2 秒音频，确认 ASR 模型能转写；测试聊天会向聊天模型发送内部测试消息，确认兜底聊天可用。测试过程不会在浏览器保存或回显 API Key，通过后再点 **保存**。

---

## 🎤 第八步：现场语音演示口令

当前正式唤醒词：

```text
主唤醒词：小诺同学
备用唤醒词：小诺小诺
```

网页里的语音唤醒是展厅演示型唤醒，需要页面保持打开，并且浏览器已经授权麦克风。它不是手机系统级后台常驻唤醒。现场建议先点击麦克风/唤醒按钮，让页面显示“小诺同学待命中”，再请领导说口令。

最稳口令：

```text
小诺同学，帮我为过敏性哮喘设计一个抗体分子，并打印一个结构模型。
```

推荐口令：

```text
小诺同学，帮我做一个肿瘤免疫治疗方向的抗体设计演示。
小诺同学，请为乳腺癌相关疾病设计一个候选抗体。
小诺同学，请为类风湿方向做一个抗炎抗体设计演示。
小诺同学，请做一次完整的抗体设计演示。
```

为了防止演示内容和后端写死流程不一致，第一版只开放安全白名单路线：IL-33/ST2、PD-1/PD-L1、HER2、TNF。遇到肺癌、EGFR、IL-23、IL-17、银屑病、炎症性肠病等暂未纳入第一版演示白名单的方向，页面不会说“不支持”，会自动选择当前最适合展示的 IL-33/ST2 代表性示范路线，并显示“可用于 3D 打印的结构模型”，不会声称已经发送到真实打印机。

---

## 📸 图解关键步骤

### GitHub 创建仓库
```
GitHub 首页 → 右上角 + → New repository
```

### Railway 部署
```
Railway 首页 → New Project → Deploy from GitHub repo → 选择仓库
```

---

## 🎯 完成！

现在你的网页已经部署到互联网上了！

**访问链接格式**：
```
https://zoonoab-production.up.railway.app
```

**可以分享给任何人**，他们都能通过链接访问你的 AI 抗体设计平台！

---

## 💡 提示

- Railway 免费额度：$5/月（足够个人使用）
- 如需自定义域名，可在 Railway Settings → Domains 中设置
- 查看使用量：Railway Dashboard → Usage

---

**部署过程中遇到问题？**
- 确保所有文件都已上传到 GitHub
- 检查 Railway 部署日志
- 参考完整文档：DEPLOY.md
