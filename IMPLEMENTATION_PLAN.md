# ZoonoAb 展示轨迹与动态结构实施计划

## 1. 用户需求优先级

本计划首先满足展示系统的现场体验，不把系统扩展为现实实验、临床决策或完整生物研究平台。

用户的核心需求按优先级排序如下：

1. 用户提交自然语言分子设计需求后，页面应立即出现连续、专业、与当前需求相关的分析轨迹，体验接近成熟 AI 产品的思考流。
2. 分析轨迹允许由现有大模型生成并由后台模拟流式播放，不要求每一条展示文本都对应一次实时联网检索。
3. 展示轨迹不得与最终靶点矛盾。最终靶点、选择理由、候选靶点、机制和工作流字段继续以当前权威模型解析结果为准。
4. 两次模型调用应并行执行：展示轨迹调用负责叙事，权威解析调用负责最终业务字段，以缩短用户感知等待时间。
5. 未知靶点本地没有精确结构时，应在工作流播放期间准备正确靶点的抗原坐标，优先保证抗原身份和整体形状正确。
6. 若没有现成抗原-抗体复合物，可以使用真实抗原结构和本地 Fab/VHH 支架生成展示级结合姿态；应保证朝向合理、距离合理、无明显穿模。
7. 每轮修改都必须可回滚、可测试，并提供本机可直接打开的 localhost 链接。
8. 默认只修改本地项目和当前 GitHub 分支，不上传公司服务器或阿里云，不替换线上版本，不重启线上服务。

## 2. 当前回滚基线

- 基线提交：`a5c7d878397d9dd36c0a097e66ca18414fdebff9`
- 当前分支：`codex/voice-assistant-demo-20260528`
- 本地回滚标签：`codex/rollback-before-research-trace-20260714`
- 动态结构改造前提交：`93e6aa23865604880cd0aebfc3dbdc527597c76e`
- 动态结构改造前本地标签：`codex/rollback-before-dynamic-structure-20260714`
- 标签只保存在本地，不作为服务器部署标记。

查看基线：

```bash
git show --stat codex/rollback-before-research-trace-20260714
```

需要回看旧版本时，优先新建临时分支，不直接破坏当前工作区：

```bash
git switch -c codex/review-pre-research-trace codex/rollback-before-research-trace-20260714
```

## 3. 总体数据流

```text
用户需求
  -> 立即显示本地安全开场状态
  -> 并行调用 A：展示轨迹规划
  -> 并行调用 B：权威意图与靶点解析
  -> B 的结果成为唯一业务真值
  -> A 的占位符使用 B 的疾病、靶点、机制和抗体类型填充
  -> WebSocket 按阶段模拟流式播放
  -> 同时准备当前靶点的结构资源
  -> 工作流末尾展示正确抗原或展示级抗原-抗体姿态
```

## 4. 调用职责与一致性规则

### 调用 A：展示轨迹规划

调用 A 只生成结构化展示脚本，不决定最终靶点。为避免模型在自由文本中夹带错误的中文或英文靶点，实际协议使用受控 `action` 枚举和 `variant`，由服务端映射为安全的观众可见文案；模型决定阶段动作、顺序、Agent 和表达变体，但不能直接写入业务实体。

输出分为：

- `opening`：选择需求拆解、评估维度和输入准备等不依赖靶点的动作。
- `afterTarget`：选择靶点背景、抗原可及性和机制一致性动作，服务端模板只使用 `{{target}}`、`{{disease}}`、`{{mechanism}}` 占位符。
- `structure`：选择结构准备、抗原表面检查和姿态元信息动作，服务端模板只使用 `{{target}}` 和 `{{antibodyType}}` 占位符。

调用 A 不得生成具体论文标题、DOI、PDB ID、UniProt ID、实时网站数量或最终靶点结论，避免生成内容与调用 B 冲突。

### 调用 B：权威业务解析

调用 B 沿用当前模型优先路由，继续返回：

- 疾病方向和需求摘要。
- 最终靶点和基因名。
- 选择理由和候选靶点池。
- 作用机制、抗体类型和候选数量。
- 结构域、表位策略、结构依据和模型展示说明。

当 A 与 B 发生冲突时，无条件以 B 为准；无法安全替换的 A 步骤直接丢弃并使用本地路线化回退文案。

## 5. 分轮实施

### 第一轮：并行模拟研究轨迹

用户可见目标：

- 用户提交设计需求后立即出现专业分析状态，不再长时间停留在固定英文轮播。
- 展示轨迹由大模型生成不同表达，并分步骤流式出现。
- 靶点确定后，轨迹自动使用最终疾病、靶点、机制和抗体类型填充。
- 轨迹在同一次运行中保持顺序稳定，取消或开始新任务后旧轨迹不得串入新任务。
- 大模型轨迹调用失败或超时时，使用产品化本地回退轨迹，不能阻塞主工作流。

实现范围：

- 新增展示轨迹提示词、JSON 归一化、可见文案清洗和本地回退。
- 展示轨迹调用与权威解析调用并行启动。
- 新增 WebSocket `research_trace` / `research_trace_complete` 展示事件。
- 前端新增可折叠的分析轨迹面板，逐条展示 Agent、阶段和状态。
- 轨迹写入历史记录，并遵守 `clientRunId` 运行隔离。
- 保留现有 `tool_call`、`tool_result`、任务列表和完整工作流，不删减后续展示。

验收标准：

- 模拟模型测试证明两次请求在主解析完成前均已启动。
- 调用 A 返回错误靶点时，页面最终序列化结果不得出现该错误靶点。
- 调用 A 超时或返回非法 JSON 时，工作流仍能到达靶点证据阶段。
- 普通聊天不会误启动分子设计轨迹。
- 取消后开始新任务，不会收到旧任务的轨迹步骤。

### 第二轮：未知靶点结构解析与缓存

用户可见目标：

- 本地没有精确预设时，不再随机借用其他靶点抗原并标成当前靶点。
- 工作流期间自动准备当前靶点的实验结构或预测结构。
- 3D 区明确区分实验复合物、实验抗原、预测抗原和参考构象。

解析顺序：

1. 现有本地精确 route/profile 结构。
2. 运行时缓存中的精确靶点结构。
3. UniProt 标准化靶点、物种和 accession。
4. PDBe/RCSB 精确 accession 对应的实验结构与 biological assembly。
5. AlphaFold DB 精确 accession 对应的预测结构。
6. 明确标注的同源或代表性结构。

每个缓存项目保存：

- 标准靶点、基因、物种和 accession。
- 来源数据库、来源 ID、来源 URL 和获取时间。
- 实验方法、分辨率、覆盖率、assembly ID 和链集合。
- 抗原链、抗体链、结构等级和校验状态。
- 文件 SHA-256、缓存路径和失败原因。

运行时缓存放在 `.runtime/structure-cache`；服务器环境后续使用持久磁盘路径。缓存、日志和密钥不得提交到 Git。

验收标准：

- 已准备路线仍使用现有稳定本地预设。
- 未知靶点只在 accession、物种和链映射通过后标记为精确结构。
- 网络不可用时优先使用缓存；没有缓存时不得把无关抗原声明为当前靶点。
- 多聚体抗原优先使用 biological assembly。

### 第三轮：展示级 Fab/VHH 结合姿态

用户可见目标：

- 只有抗原结构、没有真实抗体复合物时，仍能看到视觉合理的 Fab/VHH 候选结合姿态。
- 抗原形状来自当前精确靶点；抗体可使用本地展示支架。
- 抗体 CDR 端朝向抗原表面，界面距离合理，不出现明显穿模。

几何流程：

1. 读取真实或缓存抗原坐标。
2. 读取与 route 抗体类型一致的 Fab/VHH 支架。
3. 确定抗体 CDR 端方向和可移动刚体坐标。
4. 在抗原表面选择可及展示区域。
5. 旋转抗体，使 CDR 端朝向抗原表面法向。
6. 平移到合理接触距离。
7. 检查严重原子碰撞和整体包围盒穿插。
8. 输出组合 PDB 或在 3Dmol 中加载为两个模型。

展示级约束：

- 最近重原子距离目标约 `2.8-4.5 Å`。
- 不允许大量小于 `2.0 Å` 的原子对。
- 结果标记为“候选结合姿态”或“分子构象预览”，不冒充实验复合物。
- 连续候选可以改变抗体姿态和外观，但抗原结构保持当前靶点一致。

### 第四轮：展会稳定性与体验收口

- 预热常见靶点缓存并支持 stale-while-revalidate。
- 把结构解析放在长工作流后台执行，不阻塞前台展示节奏。
- “跳过思考”只压缩展示延时，不取消结构准备和结果校验。
- 增加超时、文件大小、host allowlist、内容格式和缓存完整性检查。
- 完成桌面、移动端、连续运行、断线、取消、无 Key 和无网络测试。
- 完成 3D 截图和画布像素验证，确保 viewer 非空、构图正确且无 UI 遮挡。

### 第五轮：分子模型加载性能

- 对 PDB、JavaScript 和 HTML 响应启用 gzip/Brotli 压缩，降低低带宽路演现场的首次传输时间。
- 本地 PDB 接口返回 `Cache-Control`、`ETag` 和 `Last-Modified`，缩略图加载后全屏 viewer 可直接复用浏览器缓存。
- 缩略图 WebGL/PDB 解析改为单队列串行，全屏 viewer 打开时暂停后续缩略图解析，避免多个大结构抢占主线程。
- 全屏 viewer 关闭后保留已加载 iframe，再次打开同一模型时不重新下载或解析。
- 受控结构 URL 加载失败时直接显示明确错误，不再执行可能额外等待 15 秒的重复下载。

## 6. 版本、提交与验证规则

- 任何影响展示、交互、后端或部署行为的修改都更新 `public/index.html` 中纯数字 `APP_BUILD_VERSION`。
- 每轮完成后运行与风险相匹配的测试。
- 每轮完成后启动或确认本地服务运行，并提供实际 localhost URL。
- 每轮文件修改后创建本地 Git commit，并按项目规则推送当前 GitHub 分支。
- 不提交 `.runtime/`、`.playwright-cli/`、`output/`、日志、PID、密钥或私有配置。
- 未获得当前任务明确授权，不执行公司服务器或阿里云部署。

## 7. 每轮需要用户细化的方向

每轮交付后只询问影响下一轮设计的关键选择，已确认需求继续执行，不重复阻塞：

- 分析轨迹更偏“专业科研叙事”还是“路演易懂叙事”。
- 轨迹面板默认展开还是自动折叠已完成阶段。
- 未知靶点无真实复合物时，优先展示抗原单体还是自动生成展示级 Fab/VHH 姿态。
- 3D 结果是否需要显式显示“实验结构 / 预测结构 / 候选姿态”分级标签。

## 8. 已确认的动态结构方案

用户已选择“真实抗原结构 + 自动生成 Fab/VHH 展示级结合姿态”，而不是只展示抗原单体。

本轮实现边界如下：

- 已准备 route/profile 继续使用与靶点一致的本地公开结构预设。
- 未准备靶点按缓存、UniProt、RCSB biological assembly、AlphaFold DB 的顺序解析。
- 只有 accession、物种和坐标链映射同时通过时，`targetVerified` 才能为 `true`。
- 有公开实验复合物时显示公开结构参考，并明确不代表当前候选序列已被实验验证。
- 只有真实抗原时，使用本地 Fab/VHH 支架生成确定性刚体展示姿态；抗原坐标保持不变。
- 展示姿态必须通过最近距离、可见接触和硬碰撞检查，并标注为 `DISPLAY / 展示姿态`。
- 展示姿态不生成或展示 ipTM、DockQ、RMSD、pLDDT 等伪结构质量指标。
- 解析或几何校验失败时使用默认 Fab/VHH 抗原-抗体代表性结构完成展示；题头和路线信息继续保留用户需求靶点，同时将 `targetVerified` 设为 `false`，明确说明坐标抗原身份未与该靶点核验。
- 结构解析作业在工作流取消、连接关闭、绝对截止时间或最终展示等待超时后立即中止；临时网络故障不写入无结构负缓存。
- 不同靶点的坐标文件和缓存记录串行原子发布，动态姿态构建或写盘失败时继续进入默认代表结构兜底。
